import type { Footer } from "@docusaurus/theme-common";

const footer: Footer = {
  style: "dark",
  links: [
  ],
  copyright: `Copyright © ${new Date().getFullYear()} VirtualClient. Built with Docusaurus.`,
};

export default footer;
